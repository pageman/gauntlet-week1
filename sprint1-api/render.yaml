services:
  - type: web
    name: gauntlet-week1
    runtime: python
    pythonVersion: 3.11
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_PATH
        value: /tmp/tasks.db
      - key: ALLOWED_ORIGINS
        value: https://gauntlet-week1.onrender.com
      - key: ENVIRONMENT
        value: production
      - key: API_AUTH_TOKEN
        sync: false
    healthCheckPath: /health
    healthCheckInterval: 30
    healthCheckTimeout: 10
    healthCheckStartPeriod: 5
    healthCheckFailureThreshold: 3
